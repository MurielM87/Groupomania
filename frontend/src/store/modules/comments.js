const state = {
    comment: {
        content: ""
      },
      comments: [],
};

const getters = {
    allComments: (state) => state.comments
};

const actions = {};

const mutations = {

};

export default {
    state,
    getters,
    actions,
    mutations
}